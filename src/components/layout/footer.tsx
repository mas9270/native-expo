import { Link } from "lucide-react";
import { ToggleTheme } from "../custom/toggleTheme";

export default function Footer() {
  return (
    <footer className="w-full py-3 flex justify-center">
      <div className="w-full max-w-5xl px-3">فوتر</div>
    </footer>
  );
}
